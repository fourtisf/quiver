{
  "name": "quiver-monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "Quiver — the liquidity layer for Robinfun tokens on Robinhood Chain",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
